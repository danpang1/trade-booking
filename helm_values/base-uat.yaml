
# UAT environment overrides.
# UAT shares the same image artifact as prod (built once on main).
# Differences live below: ingress host and env-level config only.

ingress:
  host: test-jp-trade-booking.internal.tokkalabs.com

env:
  ENVIRONMENT: uat
